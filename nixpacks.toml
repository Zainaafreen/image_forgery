[phases.setup]
nixPkgs = ["python311", "libstdcxx5", "libGL", "libglib"]

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "python -m pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = "gunicorn image_forgery.wsgi:application --bind 0.0.0.0:$PORT"